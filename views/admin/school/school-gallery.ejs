<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="../../static/app/css/admincss/gallery.css" />
    <title>Document</title>
</head>
<body>
    <h1>Here is the gallery</h1>
    <div class="container">
        <div class="galleryBox">
            <form id="gallery-img">
                <input type="file" name="scl-gallery" multiple="true" /><br>
                <button class="btn" type="button" onclick="insertGalleryImage('school')">Submit</button>
            </form>
        </div>
        <div class="top">
            <ul>
                <% results?.forEach((el)=>{ %>
                    <li><img src="/static/<%= el.img_link %>" id="<%= el.img_id %>" onclick="deleteSchoolGalleryImage(this, 'school')"></li>
                <% }) %>
                 
              </ul>
          </div>
      </div>

      <script src="../../static/app/js/gal.js"></script>
</body>
</html>